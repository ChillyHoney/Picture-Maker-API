# README

# Ruby version
``` ruby:2.6-buster ```

# How to start

``` docker-compose up --build ```

# How to run the test suite

if using docker
``` docker-compose run web bundle exec rspec -f d ```

