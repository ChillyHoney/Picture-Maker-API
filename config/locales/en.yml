en:
  activerecord:         
    errors:      
      format: "%{attribute} %{message}"
      messages:              
        invalid_password: cannot contain white spaces (instead use '_' or '-')
        invalid_username: cannot contain special characters
